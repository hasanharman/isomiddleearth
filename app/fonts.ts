import { Bilbo_Swash_Caps, Inter } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const bilboSwashCaps = Bilbo_Swash_Caps({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});
